import React, {Component} from 'react';
import {Text, StyleSheet, View} from 'react-native';

class AccoumulatePointScreen extends Component {
  render() {
    return (
      <View>
        <Text> export default class AccoumulatePointScreen </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
export default AccoumulatePointScreen;
